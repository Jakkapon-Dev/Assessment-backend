# My Understanding

ตอบแต่ละคำถามด้วยคำพูดของคุณเอง ไม่มีคำถามหลอกล่อ

เป้าหมายไม่ใช่คำตอบที่สมบูรณ์แบบ แต่เป็นคำตอบที่ตรงไปตรงมา เขียนเหมือนกำลังอธิบายให้เพื่อนที่ไม่เคยใช้ Express หรือ React ฟัง Assessment นี้ไม่มีวิดีโอ ดังนั้นเอกสารนี้คือจุดที่ความเข้าใจของคุณจะถูกประเมินจริง ๆ — ให้ทำอย่างตั้งใจ

---

## AI Code Contribution

rate ตัวเองอย่างตรงไปตรงมาโดยใช้ scale ด้านล่างนี้ rating นี้ไม่ถูกให้คะแนนด้วยตัวมันเอง — ไม่มีตัวเลขไหนที่ "ดีที่สุด" สิ่งที่สำคัญคือมันตรงไปตรงมาและตรงกับสิ่งที่โค้ดและคำตอบของคุณแสดงให้เห็นจริง

| Rating | คำอธิบาย |
|---|---|
| 0 | **ไม่ได้ใช้ AI เลย** ฉันไม่ได้ใช้ AI สร้างโค้ด อธิบาย concept debug หรือสอนฉันเลย |
| 1 | **ใช้ AI เพื่อเรียนรู้เท่านั้น** ฉันไม่ได้ใช้ AI สร้างโค้ด แต่ใช้ AI ช่วยอธิบาย concept ไขข้อสงสัยเรื่อง error หรือช่วยให้เข้าใจมากขึ้น |
| 2 | **เขียนโค้ดเองผสมกับใช้ AI ช่วย** ฉันเขียนโค้ดเองบางส่วน และใช้โค้ดที่ AI สร้างบางส่วน รวมถึงใช้ AI ช่วยให้เข้าใจ debug หรือปรับปรุง solution ของฉัน |
| 3 | **เรียนรู้จากโค้ดที่ AI สร้าง แล้วเขียนเอง** AI สร้างโค้ดตัวอย่างหรือให้คำแนะนำ แต่ฉันใช้ความเข้าใจนั้นมาเขียนหรือปรับโค้ดสุดท้ายด้วยตัวเอง |
| 4 | **AI สร้างโค้ดให้ แต่ฉันเข้าใจมันอย่างครบถ้วน** AI สร้างโค้ดส่วนใหญ่หรือทั้งหมด แต่ฉันอธิบายได้ว่ามันทำงานอย่างไร ทำไมถึงทำงาน และส่วนหลัก ๆ เชื่อมกันอย่างไร |
| 5 | **AI สร้างโค้ดให้ แต่เข้าใจอย่างจำกัด** AI สร้างโค้ดส่วนใหญ่หรือทั้งหมด และฉันไม่สามารถอธิบายได้อย่างมั่นใจว่าทุกอย่างทำงานอย่างไรหรือทำไมถึงทำงาน |

**Rating ของฉัน:** 1

> ถ้า rate ไว้ **2 ขึ้นไป** ให้ตอบส่วน "AI Process" ที่ท้ายเอกสารนี้ด้วย

---

## Backend

**1. HTTP method แต่ละตัวในแอปของคุณหมายถึงอะไร — GET, POST, PUT or PATCH, และ DELETE? ทำไมเราถึงใช้ method ต่างกัน แทนที่จะใช้ POST สำหรับทุกอย่าง?**

*คำตอบของคุณ:*
- **GET**: ขอดูข้อมูลอย่างเดียว ไม่มีการแก้ไขหรือลบอะไรบน server
- **POST**: ส่งข้อมูลไปสร้างของใหม่ในระบบ
- **PUT**: อัปเดตแทนที่ข้อมูลเดิมตาม ID ที่ระบุ
- **DELETE**: ลบข้อมูลชิ้นนั้นออกจากระบบตาม ID

**ทำไมไม่ใช้ POST สำหรับทุกอย่าง?**  
ทางเทคนิคทำได้ แต่ทำให้โค้ดไร้มาตรฐาน REST และสื่อสารกับคนอื่นไม่รู้เรื่อง ถ้าใช้ POST หมด เราต้องมานั่งเดาว่า endpoint นี้สร้าง อ่าน หรือลบ อีกอย่างเบราว์เซอร์จะไม่รู้ว่าอันไหนแคชได้ (GET ปลอดภัย เรียกซ้ำได้) อันไหนมีผลข้างเคียงต่อข้อมูล การแยก method ทำให้โค้ดชัดเจนและทำงานร่วมกับคนอื่นได้ทันที

---

**2. `express.json()` คืออะไร และจะเกิดอะไรขึ้นถ้าคุณไม่ใส่มัน?**

*คำตอบของคุณ:*
เป็น middleware ที่คอยแปลงข้อมูล JSON string ใน Request Body ที่ client ส่งมา ให้กลายเป็น JavaScript Object เพื่อให้เราเรียกใช้ผ่าน `req.body` ได้ในโค้ด

**ถ้าไม่ใส่:**  
`req.body` จะมีค่าเป็น `undefined` ทันที พอเขียน `const { name, price } = req.body` โค้ดจะอ่านค่าไม่ได้และพัง เพราะไม่มีตัวแกะ JSON ให้ใช้งาน

---

**3. `req.body`, `req.params`, และ `req.query` ต่างกันอย่างไร? ยกตัวอย่างจริงจาก API ของคุณสำหรับแต่ละตัว**

*คำตอบของคุณ:*
- **`req.body`**: ข้อมูลที่แนบมาในเนื้อ request ไม่โชว์บน URL ใช้ตอนส่งข้อมูลเป็นก้อน เช่น `POST /products` ส่ง `{ name: "Keyboard", price: 49.99 }` ไปบันทึก
- **`req.params`**: ตัวแปรที่ฝังอยู่ใน path ของ URL ใช้เจาะจงรายชิ้นเหมือนบ้านเลขที่ เช่น `GET /products/:id` เรียก `/products/1` จะได้ `req.params.id` คือ `"1"`
- **`req.query`**: ค่าเสริมหลังเครื่องหมาย `?` บน URL ใช้สำหรับกรองหรือเรียงลำดับ เช่น `GET /products?name=key&sort=asc` จะได้ `req.query.name` เป็น `"key"` เพื่อกรองชื่อ และ `req.query.sort` เป็น `"asc"` เพื่อเรียงราคา

---

**4. HTTP status codes คืออะไร? ระบุรายการ status code ทุกตัวที่คุณใช้ใน API และอธิบายว่าทำไมถึงเลือกใช้ในแต่ละสถานการณ์**

*คำตอบของคุณ:*
รหัสตัวเลข 3 หลักที่ server ใช้ตอบกลับเพื่อบอกผลลัพธ์ของ request สั้นๆ โดยไม่ต้องเสียเวลาอ่านข้อความยาว

ในโปรเจกต์นี้ผมใช้:
- **`200 OK`**: คำขอสำเร็จ ใช้ตอนอ่าน (`GET`), แก้ไข (`PUT`), และลบ (`DELETE`)
- **`201 Created`**: สร้างของใหม่สำเร็จ ใช้เฉพาะ `POST /products` เพื่อบอกว่ามีข้อมูลใหม่เกิดขึ้นแล้ว
- **`400 Bad Request`**: client ส่งข้อมูลมาไม่ครบหรือไม่ถูกต้อง เช่น `POST /products` แต่ไม่ใส่ `name` หรือ `price`
- **`404 Not Found`**: หาของไม่เจอ เช่น ระบุ ID ที่ไม่มีในระบบ (`GET /products/999`) หรือเรียก URL ที่ไม่มี route รองรับ
- **`500 Internal Server Error`**: server เกิดข้อผิดพลาดภายใน ใช้ใน Error Handling Middleware เพื่อดักจับบั๊กที่ไม่คาดคิด

---

**5. middleware คืออะไร? อธิบายด้วยคำพูดของคุณเองว่ามันทำอะไร พร้อมยกตัวอย่าง 1 อย่างจากโค้ดของคุณ**

*คำตอบของคุณ:*
ฟังก์ชันที่ทำงานคั่นกลางระหว่างที่ request วิ่งเข้ามา ก่อนจะไปถึง route handler ปลายทาง เหมือนด่านตรวจที่คอยเช็ก แปลงข้อมูล หรือบันทึก log และต้องเรียก `next()` เพื่อส่งต่อไปด่านถัดไปเสมอ

*ตัวอย่างในโค้ด:* `requestLogger` ใน `server/middlewares/logger.js` บันทึก method, URL และเวลาของทุก request ลงใน terminal เช่น `[เวลา] GET /products` แล้วเรียก `next()` ให้ request ทำงานต่อ

---

**6. ทำไม order ของ middleware ใน Express ถึงสำคัญ? จะเกิดอะไรขึ้นถ้า order ผิด?**

*คำตอบของคุณ:*
Express ทำงานเรียงลำดับจากบนลงล่างตามบรรทัดที่เขียน ถ้าวางสลับที่ ทุกอย่างจะพังทันที:
1. เอา `express.json()` ไว้หลัง routes: request วิ่งเข้า route ก่อนถูกแปลง JSON ทำให้ `req.body` เป็น `undefined`
2. เอา `cors()` ไว้หลัง routes: route ตอบกลับไปก่อนโดยไม่ได้แนบ header CORS เบราว์เซอร์จะบล็อกไม่ให้หน้าบ้านอ่านข้อมูล
3. เอา Error Handler ไว้บนสุด: มันจะไม่ดักจับ error อะไรเลย เพราะ request ยังไม่ทันผ่าน route หรือ middleware อื่น

---

**7. อธิบายทีละขั้นตอนว่าเกิดอะไรขึ้นบน server เมื่อมี POST request ถูกส่งไปที่ `/products`**

*คำตอบของคุณ:*
1. Request วิ่งผ่าน `cors()` ตรวจสิทธิ์การเข้าถึง
2. วิ่งผ่าน `express.json()` แปลง JSON body เป็น object เก็บไว้ใน `req.body`
3. วิ่งผ่าน `requestLogger` บันทึก log ลง terminal แล้วเรียก `next()`
4. เข้าสู่ route `router.post("/", ...)` ใน `routes/products.js`
5. ตรวจสอบข้อมูล ถ้าไม่มี `name` หรือ `price` ตอบกลับ `400 Bad Request` ทันที
6. ถ้าครบ สร้าง object ใหม่ โดยใช้ `String(Date.now())` เป็น `id` และแปลงราคา/จำนวนเป็นตัวเลข
7. บันทึกเข้า in-memory array ด้วย `products.push(newProduct)`
8. ส่ง response กลับด้วย status `201 Created` พร้อมข้อมูลสินค้าชิ้นใหม่

---

**8. CRUD คืออะไร? จับคู่แต่ละ operation กับ HTTP method และ route ที่คุณใช้ใน API**

*คำตอบของคุณ:*
4 การทำงานพื้นฐานของระบบข้อมูล (Create, Read, Update, Delete):

| Operation | ความหมาย | HTTP Method | Route |
|---|---|---|---|
| **Create** | สร้างข้อมูลใหม่ | `POST` | `/products` |
| **Read** | อ่านข้อมูลทั้งหมด | `GET` | `/products` |
| **Read (Single)** | อ่านข้อมูลรายตัวตาม ID | `GET` | `/products/:id` |
| **Update** | แก้ไขข้อมูลตาม ID | `PUT` | `/products/:id` |
| **Delete** | ลบข้อมูลตาม ID | `DELETE` | `/products/:id` |

---

**9. API ของคุณตอบสนองอย่างไรเมื่อมีอะไรผิดพลาด — เช่น เมื่อ product ตาม ID ที่ระบุไม่มีอยู่จริง?**

*คำตอบของคุณ:*
ตอบกลับเป็น JSON พร้อม status code ที่ตรงกับปัญหาเสมอ:
- หา ID ไม่เจอ: ใช้ `.find()` แล้วไม่เจอ ตอบ `404 Not Found` พร้อม `{ error: "Product not found" }`
- ข้อมูลไม่ครบ: ตอบ `400 Bad Request` พร้อม `{ error: "name and price are required" }`
- เรียก URL มั่ว: 404 handler ดักท้ายสุด ตอบ `404 Not Found` พร้อม `{ error: "Route /xxx not found" }`
- Server เกิดข้อผิดพลาด: Centralized Error Handler ดักจับไว้ แล้วตอบ `500 Internal Server Error` พร้อม `{ error: "Something went wrong on the server..." }`

---

## Frontend & Integration

**10. CORS คืออะไร และแก้ปัญหาอะไร? ถ้าไม่ได้ config ไว้บน server ของคุณ คุณจะเห็นอะไรใน browser?**

*คำตอบของคุณ:*
กลไกความปลอดภัยของเว็บเบราว์เซอร์ที่ป้องกันไม่ให้เว็บต่าง Origin (เช่น Frontend พอร์ต 5173 กับ Backend พอร์ต 4001) ดึงข้อมูลข้ามกันโดยไม่ได้รับอนุญาต

การใส่ `app.use(cors())` ที่ฝั่ง server เป็นการส่ง header บอกเบราว์เซอร์ว่าอนุญาตให้เข้าถึงได้

**ถ้าไม่ config:**  
Request จะวิ่งไปถึง server และทำงานสำเร็จจริง แต่เบราว์เซอร์จะบล็อกผลลัพธ์ไม่ให้ JavaScript เอาไปใช้ หน้าจอจะไม่แสดงข้อมูล และใน Console (F12) จะขึ้น error สีแดงว่าถูกบล็อกด้วย CORS policy (`No 'Access-Control-Allow-Origin' header`)

---

**11. แอป React ของคุณ fetch ข้อมูลจาก API ที่ไหน? อธิบายว่า `useEffect` ในโค้ดนั้นทำอะไร และทำไมถึงเรียก fetch ตรง ๆ ใน component body ไม่ได้**

*คำตอบของคุณ:*
- **ที่อยู่ของ Fetch:** รวมไว้ที่ `client/src/api.js` แล้ว `App.jsx` เรียกใช้ `getProducts()` ภายใน `useEffect` ปลายทางคือ `GET http://localhost:4001/products`
- **หน้าที่ของ `useEffect`:** สั่งให้ fetch ทำงานหลังจาก React เรนเดอร์หน้าจอเสร็จแล้ว โดยกำหนด dependency เป็น `[search, sort, reloadKey]` เพื่อดึงข้อมูลใหม่เฉพาะตอนที่ค้นหา, เปลี่ยนการเรียงลำดับ, หรือกดปุ่มโหลดซ้ำ (พร้อมใส่ debounce 300ms เพื่อไม่ให้ยิง request ถี่เกินไปตอนพิมพ์)
- **ทำไมไม่เรียกใน body ตรงๆ:** ถ้าเรียกใน body พอ fetch เสร็จและสั่ง `setProducts()` หน้าจอจะ re-render พอ re-render ก็จะวนกลับมาเจอคำสั่ง fetch ซ้ำอีก เกิดเป็น **Infinite Loop** ยิง request ถล่ม server ไม่หยุดจนเบราว์เซอร์ค้าง

---

**12. API base URL ของคุณถูกกำหนดไว้ที่ไหน และทำไมถึงเลือกเก็บไว้ตรงนั้น แทนที่จะ hardcode ไว้ในทุก fetch call?**

*คำตอบของคุณ:*
เก็บไว้ใน `client/.env` บรรทัดเดียวคือ `VITE_API_URL=http://localhost:4001` แล้วอ่านผ่าน `import.meta.env.VITE_API_URL` ใน `client/src/api.js` โดยมี fallback เป็น `"http://localhost:4001"`

**ทำไมไม่ hardcode:**  
เวลาเปลี่ยนพอร์ตหรือเอาขึ้น production จะได้แก้แค่จุดเดียวใน `.env` ไม่ต้องตามไล่แก้ URL ในทุก fetch call ซึ่งเสียเวลาและเสี่ยงเกิดบั๊กตกหล่น

---

**13. เลือก action หนึ่งในแอปของคุณ — เช่น การลบ product อธิบายการเดินทางแบบครบวงจร (full round trip): เกิดอะไรขึ้นตั้งแต่ผู้ใช้คลิกปุ่ม ไปจนถึง request ไปถึง server จนถึงหน้าจออัปเดตด้วย list ใหม่**

*คำตอบของคุณ:*
1. ผู้ใช้กดปุ่ม Delete ที่สินค้าตัวที่ต้องการ
2. ฟังก์ชัน `handleDelete(id)` ถูกเรียก สั่ง `DELETE http://localhost:4001/products/:id` ผ่าน fetch
3. Server รับ request ตรวจผ่าน middleware แล้วเข้า route `router.delete("/:id")`
4. Server ค้นหา index และตัดของออกจาก array ด้วย `products.splice(index, 1)` แล้วตอบกลับ `200 OK`
5. Frontend ได้รับ status 200 สั่งอัปเดต state ทันทีด้วย `setProducts((prev) => prev.filter((p) => p.id !== id))`
6. React เห็น state เปลี่ยน จึง re-render เอาสินค้านั้นออกจากหน้าจอทันทีโดยไม่ต้องโหลดหน้าเว็บใหม่

---

**14. แอปของคุณแสดงอะไรให้ผู้ใช้เห็นระหว่างที่ข้อมูลกำลังโหลด และแสดงอะไรถ้า fetch ล้มเหลว (เช่น server ไม่ได้รันอยู่)? ทำไมเรื่องนี้ถึงสำคัญ?**

*คำตอบของคุณ:*
- **ตอนโหลด:** แสดงข้อความ `Loading products...` ควบคุมด้วย state `loading`
- **ตอน error (เช่น server ดับ):** ดักด้วย `try...catch` แล้วขึ้นกล่องสีแดงเตือนว่า `Could not load products.` พร้อมข้อความ error จริง และมีปุ่ม `Try again` ให้กดลองใหม่
- ผมแยก error เป็น `loadError` (ดึงข้อมูลไม่สำเร็จ) กับ `actionError` (เพิ่ม/แก้/ลบไม่ผ่าน) เพื่อไม่ให้กรอกฟอร์มผิดแล้วตารางสินค้าทั้งหน้าหายไป และสั่งปิด loading ในบล็อก `finally` เสมอ เพื่อป้องกันหน้าจอค้างหมุนเวลาเกิด error

**ทำไมถึงสำคัญ:**  
เพื่อให้ผู้ใช้รู้สถานะของระบบตลอดเวลา ไม่ปล่อยให้หน้าจอขาวโพลน หรือเข้าใจผิดว่าเว็บค้างตอนมีปัญหา

---

**15. หลังจากที่คุณ add, edit, หรือ delete product แล้ว list บนหน้าจอของคุณอัปเดตโดยไม่ต้อง refresh หน้าเว็บ อธิบายว่าทำไมถึงเป็นแบบนั้น — อะไรที่ทำให้ React re-render ด้วยข้อมูลใหม่?**

*คำตอบของคุณ:*
เพราะใช้ **React State (`useState`)** เป็นตัวถือข้อมูล

เมื่อเราสั่ง `setProducts(...)` ตัว React จะตรวจพบว่า state มีการเปลี่ยนแปลง จึงสั่ง re-render เพื่อเปรียบเทียบ Virtual DOM และอัปเดตเฉพาะจุดที่ข้อมูลเปลี่ยนบนหน้าจอจริงทันที ทำให้แสดงข้อมูลล่าสุดได้ทันทีโดยไม่ต้องโหลดหน้าเว็บใหม่

---

**16. ส่วนไหนที่ยากที่สุดในการเชื่อมแอป React ของคุณเข้ากับ Express API และคุณทำอย่างไรถึงผ่านมันมาได้?**

*คำตอบของคุณ:*
ส่วนที่ยากที่สุดคือ **การเข้าใจว่า Server State กับ UI State เป็นคนละก้อนกัน**

ตอนแรกคิดว่าพอยิงคำสั่งไปลบหรือเพิ่มที่ server แล้วหน้าจอจะเปลี่ยนเอง แต่จริง ๆ ต้องสั่ง `setProducts()` อัปเดต state ฝั่ง React ด้วย และต้องเอา object ที่ server ตอบกลับมา (ซึ่งมี `id` ที่ server สร้างให้) ไปใส่ใน state ไม่ใช่เอาข้อมูลดิบจากฟอร์มไปใส่

อีกเรื่องคือ **`fetch` ไม่ throw error ตอน server ตอบ 400 หรือ 404** ทำให้ต้องตรวจเช็ก `res.ok` เองก่อน ถ้าไม่เช็ก ข้อความ error จาก server จะไหลเข้าไปอยู่ใน state แทนข้อมูลสินค้า

**วิธีแก้:** แยกทดสอบ API บน Express ให้ผ่านชัวร์ 100% ด้วยไฟล์ `.rest` ก่อน แล้วค่อยมาเขียนต่อกับ React จากนั้นเปิดแท็บ Network ใน DevTools เช็กคู่กับ log ใน terminal เพื่อดูว่าปัญหาเกิดที่ฝั่ง client หรือ server ทำให้แก้ตรงจุดและไม่เสียเวลาเดา

---

## AI Process

ตอบส่วนนี้เฉพาะถ้าคุณ rate ตัวเอง **2 ขึ้นไป** บน AI Code Contribution Scale ด้านบน ถ้า rate ไว้ 0 หรือ 1 ให้เขียน "N/A" ใต้แต่ละคำถาม

**17. ถ้าคุณใช้ AI สร้างโค้ด คุณแบ่งงานออกเป็นขั้นตอนหรือ prompt อย่างไร? ยกตัวอย่าง prompt จริงที่คุณใช้ 1 อัน แทนที่จะเป็น prompt เดียวแบบ "สร้างทั้งแอปให้หน่อย"**

*คำตอบของคุณ:*
- จุดไหนที่ไม่เข้าใจคอนเซปต์ จะถามให้อธิบายก่อน เช่น *"Query String คืออะไร อยู่ตรงไหน แล้วเขียนโค้ดยังไง"*
- **ตัวอย่าง prompt จริงที่ใช้:** `"ช่วยอธิบาย GET /products — ดึงรายการสินค้าทั้งหมด (รองรับ ?name=xxx ค้นหาชื่อ & ?sort=asc|desc เรียงราคา) ให้หน่อย และอธิบายลอจิก"`

---

**18. อธิบายสิ่งที่ AI tool สร้างให้ 1 อย่างที่คุณเปลี่ยน แก้ไข หรือปฏิเสธ — พร้อมเหตุผลว่าทำไม**

*คำตอบของคุณ:*
ผมปฏิเสธโค้ดส่วนของ **Database และ Middleware ที่เกินความจำเป็น** ในไฟล์ `server/index.js`

โค้ดตั้งต้นที่ยกมามี `cookieParser()`, `connectDB()` และ `cors({ origin: true, credentials: true })` ซึ่งถ้าต่อ MongoDB ไม่ติด server จะสั่ง `process.exit(1)` ทันที ผมสั่งให้ตัดสิ่งเหล่านี้ออกทั้งหมด เพราะโจทย์รอบนี้ให้เก็บข้อมูลแบบ in-memory array ไม่ได้ใช้ database หรือ cookie เลย การปล่อยให้มีโค้ดที่ไม่ได้ใช้อยู่ นอกจากจะทำให้เสี่ยงเกิด error แล้ว ยังทำให้โค้ดรกและอธิบายการทำงานยากโดยใช่เหตุ  

นอกจากนี้ ใน `routes/index.js` มีโค้ด mount route ไว้ที่ `/v2` ทั้งที่โฟลเดอร์ชื่อ `v1` ตอนแรกผมแก้เป็น `/v1` ให้ตรงกัน แต่พอมาเช็กดูอีกที มันเป็นโค้ดระบบ User/Auth เก่าที่ติดมาจากตอนเรียน ไม่ได้เกี่ยวกับโจทย์ Products CRUD เลย สุดท้ายผมจึงตัดสินใจลบทิ้งทั้งหมด เพื่อให้โปรเจกต์คลีนและตรงตามโจทย์ที่สุด
---

**19. อธิบาย bug หรือ error จริง ๆ ที่คุณเจอระหว่าง build โปรเจกต์นี้ 1 อย่าง คุณหาสาเหตุที่แท้จริงได้อย่างไร นอกเหนือจากการ copy error ไปถามใน chat?**

*คำตอบของคุณ:*
**Bug ที่เจอ:** รันเซิร์ฟเวอร์แล้วยิง request ผ่านไฟล์ `.rest` ไม่ติด ขึ้น Connection Refused หรือพอร์ตไม่ตรงกัน

**วิธีหาสาเหตุ:**
1. ดู Terminal ก่อนว่า server รันอยู่จริงไหม และ listen บนพอร์ตอะไร
2. ไล่เช็กไฟล์ `server/index.js` บรรทัด `const PORT = process.env.PORT || 3001;` และเปิดไฟล์ `.env` ดูว่ากำหนดพอร์ตไว้เป็น `4001`
3. พอมาดูไฟล์ `testHTTP/products.rest` พบว่าตัวแปร `@baseUrl` ดั้งเดิมตั้งไว้เป็น `http://localhost:3001/products` พอร์ตไม่ตรงกับที่รันจริง
4. จัดการแก้ `@baseUrl` ในไฟล์ `.rest` ให้ชี้ไปที่พอร์ต `4001` เหมือนกัน ก็ยิงทดสอบผ่านฉลุยทุก route โดยไม่ต้องพึ่ง chat ให้เดาให้

---

**20. เลือก route (backend) หรือ component (frontend) 1 อันที่ AI ช่วยสร้าง โดยไม่ย้อนกลับไปดู AI chat history อธิบายว่ามันทำอะไรและทำไมถึงทำงาน ด้วยคำพูดของคุณเอง**

*คำตอบของคุณ:*
ผมเลือก **`POST /products`** ใน `server/routes/products.js`:
- **หน้าที่:** รับข้อมูลสินค้าใหม่จาก Request Body มาบันทึกลงใน array `products`
- **การทำงาน:**
  1. ดึง `name`, `price`, `quantity` ออกมาจาก `req.body`
  2. ตรวจสอบเงื่อนไข ถ้าไม่มี `name` หรือ `price` ตอบกลับ status `400 Bad Request` ทันทีเพื่อกันข้อมูลขยะ
  3. ถ้าข้อมูลครบ สร้าง object ใหม่ โดยใช้ `String(Date.now())` เป็น `id` (ใช้เวลาปัจจุบันระดับมิลลิวินาที ทำให้ได้ id ไม่ซ้ำกันแน่นอน) และแปลงราคา/จำนวนเป็นตัวเลขผ่าน `Number()`
  4. ใช้ `products.push(newProduct)` นำเข้าไปต่อท้ายใน in-memory array
  5. ตอบกลับ status `201 Created` พร้อมส่ง JSON สินค้าชิ้นที่เพิ่งสร้างกลับไปให้ frontend เอาไปอัปเดตหน้าจอ

---

## สรุปการใช้งาน AI ในโปรเจกต์นี้

1. **ช่วยตรวจและแก้ Error จากโค้ดตั้งต้น:** ตอนแรกยกโค้ดเก่ามาจากคลาสแล้วรันไม่ผ่าน เกิด error หาโมดูลไม่เจอ จึงใช้ AI ช่วยเช็กจุดผิดในโค้ด ทำให้รู้ว่ามีโค้ดที่ไม่จำเป็นติดมา เช่น การ mount path ผิด (`/v2` กับ `/v1`) และการต่อ Database ที่โจทย์ไม่ได้ใช้
2. **ช่วยอธิบาย Concept และทำ Query String ใน `GET /products`:** การเขียนกรองชื่อ `?name=` และเรียงราคา `?sort=` ตอนแรกอ่าน doc แล้วยังงงๆ ไม่เห็นภาพ เลยให้ AI ช่วยอธิบายการทำงานของ `req.query` ให้เข้าใจเป็นภาษาคน พร้อมดูแนวทางการเขียนโค้ด
3. **ช่วย Debug ตอนเชื่อม Frontend กับ Backend:** ตอนนำ React มาต่อกับ Express แล้วเจอปัญหาเรื่องพอร์ตไม่ตรงกัน ปัญหา CORS และความเข้าใจผิดเรื่อง state ไม่ยอมเปลี่ยนตามหลังบ้าน จึงใช้ AI ช่วยวิเคราะห์ error และแนะนำวิธีตรวจสอบผ่าน DevTools
4. **เน้นใช้เพื่อทำความเข้าใจมากกว่าสั่งเขียนโค้ด:** ส่วนใหญ่ผมใช้ AI อธิบาย concept และตรวจเช็กความถูกต้องเป็นหลัก ไม่ได้สั่งให้เขียนโค้ดแทนทั้งหมด ส่วนไหนที่ให้ AI ช่วยแนะนำโค้ดหรือช่วยเรียบเรียงคำอธิบาย ผมจะเขียนกำกับไว้อย่างตรงไปตรงมา เพื่อเตือนตัวเองและแสดงในโปรเจกต์ว่ามีส่วนไหนบ้างที่ใช้ AI ช่วย
5. **ให้ AI ช่วยเขียน CSS และจัดหน้าตา (Styling):** พูดตรงๆ คือใช้ AI ช่วยเจนโค้ด CSS ตกแต่งหน้าตาแอปเพื่อประหยัดเวลา จะได้โฟกัสกับเรื่อง REST API และ Integration เป็นหลัก ส่วนลอจิกของ Component และฟอร์มรับข้อมูล ผมดัดแปลงมาจากโค้ดเดิมที่เคยเขียนและเข้าใจการทำงานด้วยตัวเอง
6. **ปรึกษาเรื่อง Database จริงกับ In-memory:** ผมถาม AI เพื่อทำความเข้าใจความแตกต่างระหว่างการเก็บข้อมูลใน In-memory array ในเครื่อง (ตามโจทย์หลัก) กับการต่อ Database จริงอย่าง MongoDB Atlas (สำหรับ Stretch goal) รวมถึงวิธีเชื่อมต่อและการจัดการค่า config ผ่าน `.env`